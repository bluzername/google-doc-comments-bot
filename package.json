{
  "name": "google-doc-comments-bot",
  "version": "1.1.0",
  "description": "Google Apps Script bot that answers @AI comments in a Google Doc using Claude",
  "private": true,
  "license": "MIT",
  "scripts": {
    "lint": "node -e \"const fs=require('fs');new (require('vm').Script)(fs.readFileSync('src/Code.gs','utf8'),{filename:'Code.gs'});JSON.parse(fs.readFileSync('src/appsscript.json','utf8'));console.log('lint ok')\"",
    "test": "node --test \"test/**/*.test.js\"",
    "push": "clasp push",
    "open": "clasp open"
  },
  "devDependencies": {
    "@google/clasp": "^3.4.1"
  }
}
